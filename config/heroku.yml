#
# Format:
# 
# <heroku_san shorthand name>:
#   app: <Heroku app name>
#   config:
#     - <Heroku config:var name>: <Heroku config:var value>
#
production: 
  app: thekidreport

staging:
  app: thekidreport-staging
  config: &default
    BUNDLE_WITHOUT: "development:test"